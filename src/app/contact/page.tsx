import React from 'react'
import { Metadata } from 'next'

export const metadata = ({ title : "Contact | SYCNEX"}) as Metadata

const page = () => {
  return (
    <div>contact</div>
  )
}

export default page